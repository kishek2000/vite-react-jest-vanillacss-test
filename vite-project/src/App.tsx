import { buttonStyle, pageContainer } from "./styles/app.css";

const App = () => {
  return (
    <div className={pageContainer}>
      <button className={buttonStyle}>Click Me!</button>
    </div>
  );
};

export default App;
